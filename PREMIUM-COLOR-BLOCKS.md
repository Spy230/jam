# Премиум цветовые блоки разделов — Реализовано ✅

## Обзор
Реализована премиум-стилизация всех разделов сайта с четкой визуальной сегментацией в стиле дорогих SPA и бутиков. Каждый раздел имеет уникальный фон и тонкую градиентную линию сверху для создания эффекта перехода между зонами.

---

## Цветовая палитра

### Основные цвета:
- **Черный**: `#000000` (чистый черный)
- **Почти черный 1**: `#0d0d0f` (темно-серый)
- **Почти черный 2**: `#0a0a0c` (еще темнее)
- **Бордовый**: `#c2185b`
- **Розово-красный**: `#ff2a6d`
- **Золотой**: `#d4af37`

### Градиенты:
- **Акцентная линия**: `linear-gradient(90deg, #ff2a6d, #d4af37)`
- **Контакты (эмоциональный)**: `linear-gradient(135deg, #0b0010, #1a0010)`
- **Мастера (полупрозрачный)**: `linear-gradient(135deg, rgba(194, 24, 91, 0.1), rgba(255, 42, 109, 0.05))`

---

## Распределение цветов по разделам

### 1. **Наши услуги** (services-trinity)
- **Фон**: `#0d0d0f` (почти черный)
- **Линия сверху**: Градиент `#ff2a6d → #d4af37` (4px)
- **Заголовок**: Белый `#ffffff`
- **Отступы**: `80px 0` (мобильные: `60px 0`)

### 2. **О нас** (about-us)
- **Фон**: `#0a0a0c` (темнее)
- **Линия сверху**: Градиент `#ff2a6d → #d4af37` (4px)
- **Заголовок**: Белый `#ffffff`
- **Отступы**: `80px 40px` (мобильные: `60px 20px`)

### 3. **Сертификаты** (certificates)
- **Фон**: `#0d0d0f` (почти черный)
- **Линия сверху**: Градиент `#ff2a6d → #d4af37` (4px)
- **Заголовок**: Белый `#ffffff`
- **Отступы**: `80px 0` (мобильные: `60px 0`)

### 4. **Наши Мастера** (masters-section)
- **Фон**: Градиент `linear-gradient(135deg, rgba(194, 24, 91, 0.1), rgba(255, 42, 109, 0.05))`
- **Линия сверху**: Градиент `#ff2a6d → #d4af37` (4px)
- **Заголовок**: Золотой `#d4af37` (для контраста с полупрозрачным фоном)
- **Отступы**: `80px 20px` (мобильные: `60px 20px`)
- **Эффект**: Карточки "всплывают" на полупрозрачном фоне

### 5. **Почему выбирают именно нас** (why-us)
- **Фон**: `#0a0a0c` (темнее)
- **Линия сверху**: Градиент `#ff2a6d → #d4af37` (4px)
- **Заголовок**: Белый `#ffffff`
- **Отступы**: `80px 0` (мобильные: `60px 0`)

### 6. **Интерьер** (interior)
- **Фон**: `#000000` (чистый черный для максимального контраста с фото)
- **Линия сверху**: Градиент `#ff2a6d → #d4af37` (4px)
- **Заголовок**: Золотой `#d4af37`
- **Отступы**: `80px 20px` (мобильные: `60px 15px`)
- **Эффект**: Чистый и воздушный, фото выглядят ярче

### 7. **Отзывы** (reviews-section)
- **Фон**: `#0d0d0f` (почти черный)
- **Линия сверху**: Градиент `#ff2a6d → #d4af37` (4px)
- **Заголовок**: Белый `#ffffff`
- **Отступы**: `80px 20px` (мобильные: `60px 20px`)

### 8. **Контакты** (map-section) — Эмоциональный акцент
- **Фон**: Градиент `linear-gradient(135deg, #0b0010, #1a0010)` (темно-бордовый с легким свечением)
- **Линия сверху**: Усиленный градиент `#ff2a6d → #d4af37 → #ff2a6d` (5px + свечение)
- **Заголовок**: Золотой `#d4af37` с эффектом свечения `text-shadow: 0 0 30px rgba(212, 175, 55, 0.5)`
- **Отступы**: `80px 0` (мобильные: `60px 0`)
- **Эффект**: Завершающий раздел с эмоциональным акцентом

---

## Ключевые особенности

### ✅ Тонкие градиентные линии
- Каждый раздел начинается с горизонтальной линии высотой **4px** (контакты — 5px)
- Градиент: `linear-gradient(90deg, #ff2a6d, #d4af37)`
- Раздел "Контакты" имеет усиленную линию с свечением

### ✅ Четкая визуальная сегментация
- Разделы чередуются между темными оттенками (`#0d0d0f` и `#0a0a0c`)
- Несоседние разделы могут иметь одинаковый фон (например, "О нас" и "Почему выбирают нас")
- Специальные разделы ("Мастера", "Контакты") имеют градиентные фоны

### ✅ Заголовки
- Основные разделы: белый цвет `#ffffff`
- Специальные разделы ("Мастера", "Интерьер", "Контакты"): золотой `#d4af37`
- Жирный шрифт `font-weight: 700`

### ✅ Вертикальные отступы
- Десктоп: `padding: 80px 0` (или с боковыми отступами)
- Мобильные: `padding: 60px 0`

### ✅ Эффект "всплывающих" карточек
- Разделы с карточками ("Мастера", "Услуги", "Отзывы") имеют полупрозрачный или контрастный фон
- Карточки выделяются на фоне раздела

### ✅ Эмоциональный акцент на "Контактах"
- Темно-бордовый градиент с легким свечением
- Усиленная градиентная линия с эффектом свечения
- Золотой заголовок с text-shadow

---

## Технические детали

### Файлы изменены:
1. **custom-styles.css** — добавлен блок "ПРЕМИУМ ЦВЕТОВЫЕ БЛОКИ РАЗДЕЛОВ"
2. **index.html** — обновлены inline-стили для разделов

### CSS-классы:
```css
/* Базовые стили для всех разделов */
section {
    position: relative;
    padding: 80px 0;
}

section::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 4px;
    background: linear-gradient(90deg, #ff2a6d, #d4af37);
    z-index: 1;
}
```

### Адаптивность:
```css
@media (max-width: 768px) {
    section {
        padding: 60px 0;
    }
}
```

---

## Результат

При скролле пользователь ощущает четкие переходы между зонами:
1. **Темные блоки** (`#0d0d0f`, `#0a0a0c`) чередуются для создания ритма
2. **Градиентные линии** визуально разделяют разделы
3. **Специальные разделы** ("Мастера", "Контакты") выделяются градиентными фонами
4. **Интерьер** на чистом черном фоне делает фото максимально яркими
5. **Контакты** завершают сайт эмоциональным акцентом с бордовым градиентом

Сайт выглядит как дорогой SPA или бутик с премиум-атмосферой! 🎨✨
